# if inside one function, you want to create local variable and global variable of the same name at the same time then only globals keyword is required.

# global keyword is used where we want to change the value of a global variable from inside a local variable.

# This tutorial is also quite important

